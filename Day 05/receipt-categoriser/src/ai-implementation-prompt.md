| # | What you asked the AI                         | What it produced                                                                   | Did it match the spec?                                                              | Your fix / follow-up                                                                          |
| - | --------------------------------------------- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 1 | First implementation pass                     | Receipt categoriser service with OCR → LLM categorisation flow and response object | Partial — invented a `tags` field that was not in the API contract                  | Removed `tags`, aligned output to `{ category, confidence, source }`, added unit tests        |
| 2 | Add fallback when LLM returns 503             | Rule-based categoriser and fallback logic                                          | Partial — fallback existed but did not satisfy AC-03 confidence/source requirements | Updated fallback to return `source: "rule-based"` and confidence ≤ 0.5, added acceptance test |
| 3 | Add OCR failure handling and acceptance tests | OCR failure response, payload validation, and Jest acceptance tests                | Yes — matched AC-04, AC-05 and AC-06 requirements                                   | Added tests for OCR failure, oversized payload (413), and telemetry PII boundary              |
